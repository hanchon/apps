import { CheckIcon } from "icons";

export const CheckMark = () => (
  <span className="rounded-full bg-red h-4 w-4 flex items-center justify-center">
    <CheckIcon width={"14px"} height={"14px"} className="text-white" />
  </span>
);
